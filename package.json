{
  "name": "my-vue-app",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "start": "sirv dist --single --host"
  },
  "dependencies": {
    "preact": "^10.5.13",
    "sirv-cli": "^1.0.11"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.0.0",
    "@types/react": "^17.0.3",
    "typescript": "^4.2.3",
    "vite": "^2.1.2"
  }
}
